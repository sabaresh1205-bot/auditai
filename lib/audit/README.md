Audit engine overview

- `pricing.ts`: Central pricing catalog (USD) + display helpers.
- `rules.ts`: Pure deterministic rules that output recommendations with numeric savings.
- `engine.ts`: Orchestrates all rules and returns an `AuditReport`.

