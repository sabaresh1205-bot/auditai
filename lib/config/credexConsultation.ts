/** External scheduling link (placeholder). Opens in a new tab from the results page. */
export const CREDEX_CONSULTATION_URL = "https://calendly.com/credex-consultation";

/** Show Credex CTA when estimated monthly savings are at least this amount (same units as the report currency). */
export const CREDEX_CTA_MIN_MONTHLY_SAVINGS = 500;
